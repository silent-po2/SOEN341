extends layout

block content
  h1 #{result} 
  //- each Message, i in result
  //-   li.Message
  form(method='POST', action='/dashboard')
    #form-group
      label Message:
      textarea.form-control(name='message')
    br
    input.btn.btn-primary(type='submit',value='Submit')

